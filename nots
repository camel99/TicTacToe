                        app.alogrithm = {
                            array: [],
                            results: [],
                            isClicked: function (index) {
                               return !!array[index];
                            },
                            push: function (index, mark) {
                                this.array[index] = mark;
                            },
                            gameOutcome: function () {
                                //robisz to samo co na buyttonach tylko na tej tablicy
                            }

                        }


                        var index  = 2;

                        if (!app.alogrithm.isClicked(index)) {
                            array[index] = 'X';

                            if (app.alogrithm.gameOutcome()) {

                            }

                        }